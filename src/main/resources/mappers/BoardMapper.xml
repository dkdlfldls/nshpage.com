<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nshpage.mapper.BoardMapper">

	
	<select id="getAllBoard" resultType="Board">
		SELECT board_id, writer, title, content, regist_time FROM board 
		WHERE state=1 AND category_id=#{category_id}
	</select>
	
	<select id="read" resultType="Board">
		SELECT board_id, writer, title, content FROM board 
		WHERE board_id = #{board_id} AND state=1 AND category_id=#{category_id}
	</select>
	
	<insert id="write" parameterType="Board">
		INSERT INTO board (user_id, writer, category_id, state, title, content, ip) 
		VALUES(#{user_id}, #{writer}, #{category_id}, 1 , #{title}, #{content}, #{ip})
	</insert>
	
	<update id="update">
		UPDATE FROM board 
		SET title=#{title}, content=#{content}
		WHERE board_id=#{board_id} AND user_id=#{user_id}
	</update>
	
	<update id="delete">
		UPDATE FROM board 
		SET state=3 WHERE board_id=#{board_id} AND user_id=#{user_id}
	</update>
	
</mapper>